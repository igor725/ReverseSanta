#include "game.hh"
// #include "engine.hh"

Game::Game() {}

Game::~Game() {}

void Game::OnUpdate(FLOAT delta) {(void)delta;
	// auto engine = Engine::GetInstance();
	// auto graphics = engine->SysGraphics();
	// auto camera = graphics->GetCamera();
	// ...
}
